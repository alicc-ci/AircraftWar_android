<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.hitsz">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AircraftWar">

        <!-- 程序入口 -->
        <activity
            android:name="edu.hitsz.application.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 开始界面 -->
        <activity
            android:name="edu.hitsz.application.BeginningActivity"
            android:exported="true" />

        <!-- 游戏界面 -->
        <activity
            android:name="edu.hitsz.application.GameActivity"
            android:exported="true" />

        <!-- 排行榜界面 -->
        <activity
            android:name="edu.hitsz.score.ScoreActivity"
            android:exported="true" />

    </application>

</manifest>